{
  "parameters": [
    { "name": "setback_shift", "label": "Extra setback added behind the boost (mm)", "type": "number", "min": 50, "max": 150, "step": 5, "default": 100 },
    { "name": "setback_clear", "label": "Spacer-to-dock clearance (mm)", "type": "number", "min": 0.2, "max": 1, "step": 0.05, "default": 0.35 },
    { "name": "stand_lift", "label": "Stand lift — must match rig link (mm)", "type": "number", "min": 0, "max": 50, "step": 1, "default": 25 },
    { "name": "base_d", "label": "Turntable base diameter — must match rig link (mm)", "type": "number", "min": 160, "max": 220, "step": 1, "default": 166 },
    { "name": "stand_gap", "label": "Gap from collar to stand dock — must match rig link (mm)", "type": "number", "min": 3, "max": 60, "step": 1, "default": 6 },
    { "name": "link_clear", "label": "Collar-to-base clearance — must match rig link (mm)", "type": "number", "min": 0.2, "max": 1, "step": 0.05, "default": 0.4 },
    { "name": "stand_w", "label": "Stand width — must match phone stand and boost (mm)", "type": "number", "min": 60, "max": 120, "step": 5, "default": 80 }
  ]
}
