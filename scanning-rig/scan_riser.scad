// Scanning Rig - Optional height/angle correction for the scan boost
// Render this file to generate scan_riser.stl
//
// Drops into the scan boost's existing foot pocket, exactly where the phone
// stand's foot sits today, and re-presents an identical pocket riser_h
// further up (#468 review) — the boost itself does not change shape.

include <_scanning_rig.scad>

scan_riser();
